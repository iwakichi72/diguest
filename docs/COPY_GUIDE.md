# Diguest — コピーガイド

## ボイス&トーン

### Diguestが話すとき

Diguestは静かで、押しつけがましくない。
余計なことを言わない。感嘆符は使わない。

**使わない語彙:**
- ようこそ、ぜひ、ぜひとも、さあ、どうぞ
- 素晴らしい、いいですね、おめでとう
- 簡単です、すぐできます
- 何でもお気軽に
- ✅ 🎉 🚀 などの絵文字全般

**使う語彙:**
- 静かな命令形（「始める」「戻る」「終わる」）
- 問いかけ（「何を掘りますか？」）
- 事実の記述（「8ターン」「2025-12-01」）

---

## 画面別コピー

### ホーム画面

| 要素 | コピー |
|------|--------|
| ロゴ/アプリ名 | `Diguest` |
| メインの問いかけ | `今日、何を掘りますか？` |
| プライマリCTA | `新しいセッションを始める` |
| セクション見出し | `過去のノート` |
| ノートが0件のとき | `まだノートはありません` |
| 設定ボタン | `設定` |

**ノートが0件のとき（説明文）:**
```
最初のセッションを終えると、ここにノートが残ります。
```

---

### 新規セッション画面

| 要素 | コピー |
|------|--------|
| 画面の見出し | `今日、何を掘りますか？` |
| 入力フィールドのラベル | （ラベルなし——見出しが兼ねる） |
| プレースホルダー | `最近の違和感、問い、テーマ…` |
| 補足テキスト | `一言でも、問いの形でも。` |
| CTAボタン | `掘り始める` |
| 戻るリンク | `← 戻る` |

**入力フィールドの下の補足文（サブテキスト）:**
```
一言でも、問いの形でも。
```

補足: 「答えが出なくても大丈夫」などの安心感を与えるコピーはあえて書かない。
ユーザーはそれを求めていない——問いを立てるための場所として提示するだけでいい。

---

### 対話画面

| 要素 | コピー |
|------|--------|
| セッション終了ボタン | `終わる` |
| 入力フィールドのプレースホルダー | `（空白のまま）` |
| キーヒント | `Shift+Enter で改行 · Enter で送信` |
| 発言カウンター | `{n}回` |
| Diguestがストリーミング中 | `（カーソルのみ、テキストなし）` |

**セッション開始直後（Diguestの最初の発言）:**
Diguestは自己紹介しない。「こんにちは」も言わない。ユーザーが入力したテーマを受けて、最初の問いを返すだけ。

セッション開始直後の画面には何も書かれていない。入力フィールドだけが待っている。

---

### Markdownプレビュー画面

| 要素 | コピー |
|------|--------|
| 戻るリンク | `← ノート一覧` |
| メタ情報の区切り | `·`（中黒）|
| メタ情報フォーマット | `2025-12-01 · llama3 · 8ターン` |
| コピーボタン | `Markdownをコピー` |
| エディタで開くボタン | `エディタで開く` |
| コピー成功後 | `コピーしました` |

**「コピーしました」の表示:**
ボタンテキストが一瞬だけ変わり、元に戻る。トースト通知は出さない。

---

### 保存済みノート一覧

| 要素 | コピー |
|------|--------|
| 月の見出しフォーマット | `12月` / `11月`（年は省略、年が変わるときだけ `2024年 12月`） |
| 日付フォーマット | `01`（日のみ。月見出しが上にある） |
| 新規セッションボタン | `+ 新しいセッション` |

---

### 設定画面

| 要素 | コピー |
|------|--------|
| 画面タイトル | `設定` |
| Ollamaモデルの項目名 | `モデル` |
| 保存先の項目名 | `ノートの保存先` |
| OllamaのURL項目名 | `Ollama URL` |
| 保存ボタン | `保存` |
| 保存完了 | `保存しました` |
| デフォルト値の表示 | `デフォルト: llama3` |

---

### エラー・空状態のコピー

#### Ollama未起動

```
Ollamaが見つかりません
```

補足テキスト:
```
localhost:11434 に接続できませんでした。
Ollama を起動してからもう一度試してください。
```

アクション: `もう一度試す`

**使わないコピー:** 「申し訳ありません」「エラーが発生しました」「予期しないエラー」

---

#### セッション中にOllamaが切断

```
接続が切れました
```

補足テキスト:
```
Ollama との接続が途切れました。
対話内容はまだ残っています。接続を確認してください。
```

アクション: `再接続する`

---

#### Markdown生成失敗（要約生成失敗）

ユーザーへの通知は出さない——対話ログだけで保存し、要約部分は空で保存する。
ログが残ることの方が要約より重要。

---

#### ノートの読み込み失敗

```
このノートを開けませんでした
```

補足テキスト:
```
ファイルが移動または削除されている可能性があります。
```

---

## CLIコピー（ターミナル向け）

CLIはGUIより余白が少ない。コピーは短く。色は最小限（richを使う場合）。

| 状態 | 出力テキスト |
|------|-------------|
| 起動時 | `Diguest` （プロダクト名のみ） |
| Ollama接続確認中 | `connecting...` |
| 接続成功 | `ready.` |
| テーマ入力促し | `今日、何を掘りますか？ ` |
| Ollama未起動 | `Ollama が起動していません。` |
| 補足 | `  $ ollama serve` |
| 保存完了 | `→ saved: 20251201_103045_最近の違和感.md` |
| 終了 | （何も出さない、またはファイルパスのみ） |

**CLIで使わないもの:**
- ASCII アート・ロゴ
- 起動メッセージ（「セッションを始めます！」など）
- 完了メッセージに過剰な装飾（✓✓✓ など）

---

## Markdownノート内のコピー

生成されるMarkdownファイル内のコピー。AIが生成するセクションではなく、テンプレート的な固定テキスト。

| 要素 | コピー |
|------|--------|
| フッター | `_Generated by Diguest_` |
| 対話ログのユーザー発言ラベル | `**You:**` |
| 対話ログのAI発言ラベル | `**Diguest:**` |

セクション見出し（`## 対話の要約` など）はAIが生成するわけではなく、テンプレートとして固定。
AIはその下のコンテンツを生成する。

---

## 翻訳・多言語について

MVP は日本語のみ。英語UIは将来対応。
英語にするときの対応表:

| 日本語 | English |
|--------|---------|
| 今日、何を掘りますか？ | What do you want to dig into today? |
| 掘り始める | Start digging |
| 終わる | End session |
| 掘り出されたもの | What surfaced |
| 対話ログ | Conversation log |
| 対話の要約 | Summary |
